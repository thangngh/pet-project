## Task 2: Update .env.example with DB variables

**Goal:** Add per-context database environment variables to `.env.example`.

**File modified:** `backend/.env.example`

**Changes:** Added `DB_AUTH_*`, `DB_USER_*`, `DB_CATALOG_*` env vars after the existing `# Database` block.
